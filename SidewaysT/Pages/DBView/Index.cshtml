@page
@model SidewaysT.Pages.DBView.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Weapon[0].Item)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Weapon[0].Category)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Weapon[0].Modifier)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Weapon) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Item)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Modifier)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.WeaponID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.WeaponID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.WeaponID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
